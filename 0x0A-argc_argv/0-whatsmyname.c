#include<stdio.h>
#include "main.h"

/**
 * main - prints name of the program followed by new line
 * @argc: argument count
 * @argv: arguments vector
 *
 * Return: Always 0 (Success)
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
