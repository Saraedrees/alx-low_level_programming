#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print all single digits from 0 to 10
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 10; c++)
	{
		printf("%d\n", i);
	}
	return (0);
}
