#include "main.h"

/**
 * times_table - print the 9 times table, starting with 0.
 *
 * Return - no return.
 */

void times_table(void)
{
	int a, b;

	for (a = 0; a <= 9; a++)
	{
		for (b = 0; b <= 10; b++)
		{
			_putchar(i * j);
			_putchar(',');
			_putchar('\n');
		}
		_putchar('\n');
	}
}
