#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: number to be checked
 * Return: 0 (Success)
 */

int _abs(int)
{
	int n;

	if (n < 0)
	{
		r = n * (-1)
		_putchar(r);
	}
	else if (n == 0)
	{
		_putchar(n);
	}
	else
	{
		_putchar(n);
	}
		return (0);
}
